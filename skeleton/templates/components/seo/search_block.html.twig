<div class="serp-preview">
    <div class="serp-preview__url">{{ serpUrl }}</div>
    <div class="serp-preview__title">{{ serpTitle }}</div>
    <div class="serp-preview__description">{{ serpDescription|u.truncate(160, '...') }}</div>
</div>